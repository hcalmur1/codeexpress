var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function(req, res, next) {
  res.render('index', { title: 'Este es mi primer programa con nodejs y Express' });
});

module.exports = router;
